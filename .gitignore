# Food Database Files
# These are large and shouldn't be committed to git
# They should be hosted separately or loaded dynamically
public/Data/
*.json
!src/data/brandedFoods.json

# But keep the directory structure
!public/Data/.gitkeep